{
  "name": "bookbridge-mern",
  "version": "1.0.0",
  "description": "Book Bridge C2C Platform - Full Stack Application",
  "private": true,
  "scripts": {
    "dev:full": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["book", "marketplace", "mern"],
  "author": "",
  "license": "ISC"
}
